# 里程碑

| 里程碑 | 内容 | 状态 | 完成日期 | 备注 |
|---|---|---|---|---|
| M1 | 项目骨架 + SQLite + 顶部时间日期状态栏 | ✅ 完成 | 2026-09-16 | 冒烟测试 9/9 通过 |
| M2 | 实验 CRUD + 自动结束时间 + 提醒 | ✅ 完成 | 2026-09-16 | 冒烟测试 16/16 通过 |
| M3 | 实验记录 + 图片 + 每日自动记录 + 跨日更新 | ✅ 完成 | 2026-09-16 | 冒烟测试 24/24 通过 |
| M4 | 周整理 + 下周实验规划 + 导出 | ✅ 完成 | 2026-09-16 | 冒烟测试 24/24 通过 |
| M5 | 天气 + UI 美化 + 设置 + 备份 + 托盘 | ✅ 完成 | 2026-09-16 | 冒烟测试 18/18 通过 |
| M6 | 测试、打包、验收 | ✅ 完成 | 2026-09-16 | PyInstaller 打包成功，数据目录验证通过 |

## M1 记录（2026-09-16）

- 环境：Python 3.14.7 + PySide6 + markdown
- 已实现：目录骨架、11 张表全量建库、默认设置、主窗口（左导航 + 顶栏 + 卡片内容区）、
  秒级时钟、中文日期、30 秒跨日检测（day_changed 信号已就位）、亮/暗主题切换（持久化）、
  10 个原创本地 SVG 图标（currentColor 换色 + 高 DPI 渲染）
- 验证：`python tests/smoke_m1.py` 9/9 通过

## M2 记录（2026-09-16）

- 已实现：`app/models/experiment.py` 数据访问层（CRUD + 筛选 + 状态流转 + 自动计算 planned_end）、
  实验列表页（搜索/类型/状态筛选 + 列表⇄看板 + 右键菜单）、新建/编辑实验对话框（实时跨天结束时间计算）、
  实验详情页（状态流转/编辑/删除，记录区占位）、提醒服务（30s 扫描 + reminder_log UNIQUE 去重 +
  到点 5 分钟补提醒窗口）、提示音生成（wave 模块）、关闭到托盘
- 验证：`python tests/smoke_m2.py` 16/16 通过；真实平台启动探测无报错（exit 124 = timeout 正常终止）

## M3 记录（2026-09-16）

- 已实现：记录数据层（experiment_records + record_images）、每日记录（daily_logs get_or_create/保存/追加）、
  跨日结转（carry_over_unfinished 平移到新日期）、图片压缩（最长边 1920 转 JPG，存相对路径）、
  Markdown 渲染（图片语法剥离，缩略图独立展示）、详情页记录时间线（自定义卡片 + 插入/拖拽/粘贴截图 + 点击预览）、
  仪表盘每日记录编辑区（0.8s 防抖自动保存）、跨日全量刷新编排（结转→今日记录→今日实验→周整理预留→提醒重扫）
- 验证：`python tests/smoke_m3.py` 24/24 通过；真实平台启动探测无报错

## M4 记录（2026-09-16）

- 已实现：周整理页（周切换 + 实时汇总统计 + 三栏 Markdown 自动保存 + 导出 MD/HTML）、
  实验规划页（默认下周、七列视图、卡片拖拽排序/跨天/跨周、从上周复制、从模板、
  底部周总时长/每日统计/冲突红点、一键转实验回写 linked_experiment_id）、
  跨日刷新真正接入周整理与规划页、开发期双击启动脚本 启动LabLog.vbs（pythonw 隐藏窗口）
- 验证：`python tests/smoke_m4.py` 24/24 通过；真实平台启动探测无报错

## M5 记录（2026-09-16）

- 已实现：天气服务（三源回退 QWeather→OpenWeatherMap→wttr.in，QThread 子线程，本地缓存，
  失败回退缓存）、备份/恢复（zip 打包 + 恢复前校验 db 可打开 + 清理 WAL）、完整设置页
  （外观/提醒默认/结转开关/天气/数据备份）、无边框窗口 + 自绘标题栏（拖动/双击最大化/窗口按钮）、
  托盘完善（快速新建实验）、渐变色主按钮、页面切换淡入动画、提醒弹窗滑入、空状态插画、
  规划拖拽拖影+拖拽高亮+每列独立滚动、周整理三列布局
- 验证：`python tests/smoke_m5.py` 18/18 通过；真实平台启动探测无报错
